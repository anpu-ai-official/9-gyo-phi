{
  "name": "nine-gyo-phi",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A private listening library for PDFs, notes, and ideas",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:web": "node scripts/serve.mjs",
    "build": "tauri build",
    "start": "node scripts/serve.mjs",
    "start:engine": "python3 engine/server.py 8765 --no-open",
    "test": "node --test tests/frontend/*.test.js",
    "test:engine": "python3 -m pytest tests/",
    "lint": "eslint src/static/{app,audio,core,parser,storage,neural-worker,engine}.js scripts tests/frontend && prettier --check src/index.html src/static/{app.css,app.js,audio.js,core.js,parser.js,storage.js,neural-worker.js,engine.js} scripts tests/frontend",
    "format": "prettier --write src/index.html src/static/{app.css,app.js,audio.js,core.js,parser.js,storage.js,neural-worker.js,engine.js} scripts tests/frontend",
    "check": "npm run lint && npm test",
    "check:desktop": "cargo fmt --manifest-path src-tauri/Cargo.toml --check && cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0-rc.18",
    "eslint": "^9.0.0",
    "fake-indexeddb": "^6.0.0",
    "globals": "^16.5.0",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "kokoro-js": "^1.2.1"
  }
}
